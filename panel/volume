#/bin/sh

prefix=$1
suffix=$2

while [ true ]; do
    echo -n "$prefix"
    volume=$(
        pulseaudio-ctl | grep 'Volume level' | cut -d: -f2 | tr -d ' ' \
        | sed -r "s/\x1B\[([0-9]{1,2}(;[0-9]{1,2})?)?[m|K]//g"
    )
    echo -n $volume
    echo -n "$suffix"
    echo
    sleep 1
done
